# ⚡ WaldonCFscanner-python | ابزار پیشرفته بررسی VLESS با هسته Xray

[![Python 3.8+](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/downloads/)
[![Platform](https://img.shields.io/badge/platform-Windows%20%7C%20Linux%20%7C%20macOS-lightgrey.svg)]()
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

**[🇺🇸 برای مطالعه نسخه انگلیسی اینجا کلیک کنید (English Version)](README.md)**

یک اسکنر آی‌پی کلودفلر (Cloudflare) فوق‌العاده سریع و بهینه‌سازی شده، که منحصراً برای دور زدن زیرساخت‌های شدید سانسور اینترنت (مانند فایروال بزرگ / GFW) با استفاده از پروتکل‌های مدرن پروکسی مهندسی شده است.

ساخته شده توسط [@amirrezas](https://github.com/amirrezas). با الهام از کارهای MortezaBashsiz و جامعه جهانی ضد سانسور.

---

## 🚀 شروع سریع: نصب و استفاده (اجرای بدون نیاز به تنظیمات)

ما معتقدیم ابزارهای ضد سانسور باید برای همه قابل دسترس باشند. برای استفاده از این ابزار نیازی به درک محیط‌های مجازی پایتون، `pip` یا فایل‌های باینری Go ندارید. موتور این برنامه دارای یک سیستم راه‌اندازی (Bootstrap) کاملاً خودکار است.

### 🪟 نصب در ویندوز
۱. **نصب پایتون:** پایتون را از [python.org](https://www.python.org/downloads/) دانلود کنید. **نکته بسیار مهم:** در حین نصب، حتماً تیک گزینه **"Add Python to PATH"** را بزنید.
۲. **دانلود پروژه:** روی دکمه سبز رنگ **"Code"** در بالای همین مخزن گیت‌هاب کلیک کرده و **"Download ZIP"** را انتخاب کنید. پوشه را در کامپیوتر خود استخراج (Extract) کنید.
۳. **اجرای اسکنر:** Command Prompt (`cmd`) را باز کنید، به مسیر پوشه استخراج شده بروید و دستور زیر را اجرا کنید:
   ```cmd
   python scanner.py
   ```

### 🐧 لینوکس (اوبونتو / دبیان)
۱. **نصب پایتون:** ترمینال خود را باز کرده و پایتون و pip را نصب کنید:
   ```bash
   sudo apt update && sudo apt install python3 python3-pip
   ```
۲. **دانلود پروژه:**
   ```bash
   git clone [https://github.com/amirrezas/WaldonCFscanner-python.git](https://github.com/amirrezas/WaldonCFscanner-python.git)
   cd WaldonCFscanner-python
   ```
۳. **اجرای اسکنر:**
   ```bash
   python3 scanner.py
   ```

### 🍏 مک‌اواس
۱. **نصب پایتون:** ترمینال خود را باز کرده و دستور `brew install python` را اجرا کنید (نیاز به Homebrew دارد).
۲. **دانلود و اجرا:**
   ```bash
   git clone [https://github.com/amirrezas/WaldonCFscanner-python.git](https://github.com/amirrezas/WaldonCFscanner-python.git)
   cd WaldonCFscanner-python
   python3 scanner.py
   ```

### ⚙️ نحوه استفاده از تولیدکننده خودکار کانفیگ
شما می‌توانید اسکنر را شخصی‌سازی کنید تا به طور خودکار پروفایل‌های VPN آماده‌ی استفاده را بر اساس سرور شخصی شما تولید کند.

* **از طریق JSON:** قبل از اجرای برنامه، کانفیگ پایه VLESS سرور خود را در فایلی به نام `config.json` در پوشه اصلی برنامه قرار دهید.
* **از طریق URI (کلیپ‌بورد):** در حالی که برنامه در حال اجراست، لینک کپی شده‌ی خود (مثلاً `vless://...`) را مستقیماً در کادر ورودی **VLESS URI** در داشبورد ترمینال پیست (Paste) کنید.

هنگامی که اسکنر یک آی‌پی عالی پیدا کند، پوشه‌ای به نام `output_configs/` ایجاد کرده و فایل‌های آماده `.json` و یک فایل `vless_links.txt` حاوی لینک‌های پرسرعت و قابل اشتراک‌گذاری را در آن ذخیره می‌کند.

---

## 🌍 دیدگاه اخلاقی: اینترنت به عنوان یک حقوق بشر

در سال ۲۰۱۶، شورای حقوق بشر سازمان ملل متحد قطعنامه‌ای تاریخی را تصویب کرد که در آن قطع اینترنت را صراحتاً محکوم کرده و تأکید می‌کند که *"حقوقی که افراد در دنیای آفلاین دارند، باید در فضای آنلاین نیز محافظت شود."* دسترسی به اطلاعات، آزادی بیان و حریم خصوصی دیجیتال، از حقوق بنیادین بشر هستند.

این ابزار ساخته شده است زیرا هیچ دولت یا نهادی نباید این قدرت را داشته باشد که شهروندان خود را به طور مصنوعی از جامعه جهانی جدا کند. **WaldonCFscanner** به افراد این قدرت را می‌دهد تا فایروال‌های سرکوبگر را دور بزنند، در برابر سیستم‌های بازرسی عمیق بسته‌ها (DPI) مقاومت کنند و حق مسلم خود را برای دسترسی به وب آزاد پس بگیرند.

---

## 🏗️ معماری سیستم: چرا این اسکنر متمایز است؟

### مشکل اساسی: بازرسی عمیق بسته‌ها (DPI)
اسکنرهای سنتی کلودفلر بر پینگ‌های ساده ICMP یا هندشیک‌های پایه TCP تکیه می‌کنند. در حالی که این روش‌ها صرفاً در دسترس بودن یک نود (Node) را تأیید می‌کنند، مکانیسم‌های مدرن سانسور را کاملاً نادیده می‌گیرند. فایروال‌های پیشرفته از بازرسی عمیق بسته‌ها (DPI) و کاوش فعال استفاده می‌کنند. یک آی‌پی ممکن است پینگ ۲۰ میلی‌ثانیه بدهد، اما فایروال به محض تشخیص SNIهای غیرمجاز، توزیع غیرعادی بایت‌ها، یا امضاهای VPNهای قدیمی (مانند OpenVPN یا WireGuard)، فوراً اتصال TCP را قطع می‌کند.

### راهکار VLESS بر بستر TLS
**WaldonCFscanner** این مشکل را با تأیید *پروتکل واقعی تونل پروکسی* از طریق یک کانال امن رمزنگاری‌شده حل می‌کند. با قرار دادن پروتکل VLESS در داخل یک پوشش TLS، فیلتر DPI نمی‌تواند ترافیک پروکسی را از ترافیک یک کاربر معمولی که در حال بازدید از یک وب‌سایت امن HTTPS است تشخیص دهد.

```text
+-------------------+       +--------------------+       +-------------------+       +-------------------+
|                   |       |                    |       |                   |       |                   |
|   User Machine    |       |   Great Firewall   |       |  Cloudflare Edge  |       |    Uncensored     |
|   (Scanner Core)  | =====>|    (DPI Filter)    | =====>|  (Reverse Proxy)  | =====>|     Internet      |
|  [VLESS Client]   |  TLS  | Passes Inspection  | VLESS |  [VLESS Server]   | HTTP  |                   |
|                   |       |                    |       |                   |       |                   |
+-------------------+       +--------------------+       +-------------------+       +-------------------+
```

---

## ⚙️ بررسی عمیق: موتور تأیید ۴-مرحله‌ای

برای یافتن ۱۰ آی‌پی بی‌نقص و پرسرعت از میان میلیاردها ترکیب احتمالی IPv4 و IPv6 بدون پر کردن حافظه رم سیستم یا از کار انداختن روتر میزبان، این اسکنر به عنوان یک خط تولید تهاجمی، ناهمگام (Asynchronous) و آگاه به سخت‌افزار عمل می‌کند.

```text
+---------------+    +---------------+    +---------------+    +---------------+    +---------------+
|               |    |   STAGE 1:    |    |   STAGE 2:    |    |   STAGE 3:    |    |   STAGE 4:    |
|   Subnet IP   |    |   TCP Probe   |    |   TLS & SNI   |    |   1MB Speed   |    |  Xray VLESS   |
|   Generator   | -> |  (Port 443)   | -> |  (Handshake)  | -> |  (Throughput) | -> | (Verification)|
|               |    |               |    |               |    |               |    |               |
+---------------+    +---------------+    +---------------+    +---------------+    +---------------+
  (Millions)           (Thousands/sec)      (Hundreds/sec)        (Tens/sec)           (The Winners)
```

### مرحله ۱: کاوش ناهمگام TCP (لایه ۴)
* **هدف:** حذف سریع آدرس‌های IP "مرده" که در پورت ۴۴۳ پاسخی نمی‌دهند.
* **مکانیسم:** از `asyncio.open_connection` پایتون برای ارسال صدها درخواست سوکت همزمان استفاده می‌شود. به جای انتظار برای یک پاسخ کامل HTTP، Worker دقیقاً در همان میلی‌ثانیه‌ای که بسته `SYN-ACK` دریافت می‌شود، سوکت را می‌بندد.
* **محدودیت‌های همزمانی:** موتور برنامه با خواندن `os.cpu_count()` سقف ایمن سوکت‌ها را محاسبه می‌کند (به عنوان مثال، محدودیت تا ۱۰۰۰ سوکت در ویندوز به دلیل محدودیت‌های کرنل، اما باز کردن تا ۳۰۰۰ سوکت در لینوکس/مک‌اواس با استفاده از `epoll` و `kqueue`).

### مرحله ۲: تزریق TLS SNI (لایه ۷)
* **هدف:** تأیید رمزنگاری‌شده‌ی تعلق آی‌پی به کلودفلر و دور زدن مسدودسازی دامنه‌ها مبتنی بر SNI.
* **مکانیسم:** موتور یک `ssl.create_default_context()` تأیید نشده می‌سازد و یک تونل امن برقرار می‌کند. نکته حیاتی این است که یک دامنه ایرانی "سفید" (مانند `zula.ir`) را به هدر (Server Name Indication) SNI تزریق می‌کند. اگر فایروال اجازه هندشیک را بدهد، موتور یک درخواست خام `GET / HTTP/1.1` ارسال می‌کند. اگر هدرهای پاسخ حاوی امضاهای کلودفلر (یا `403 Forbidden` از لبه کلودفلر) باشد، IP به عنوان یک نقطه ورود معتبر پروکسی تأیید می‌شود.

### مرحله ۳: تست سرعت خالص پایتون (سنجش توان عملیاتی)
* **هدف:** فیلتر کردن IPهایی که به شدت محدود شده‌اند (Throttled) یا دچار از دست رفتن شدید بسته‌ها (Packet Loss) هستند.
* **مکانیسم:** یک IP پذیرفته‌شده، تحت دانلود خام HTTP یک فایل ۱ مگابایتی از `speed.cloudflare.com/__down` قرار می‌گیرد. موتور تفاوت زمانی دقیق از اولین بایت تا آخرین بخش را محاسبه می‌کند. آی‌پی‌هایی که نتوانند یک آستانه بالای مگابیت بر ثانیه (Mbps) را حفظ کنند بلافاصله کنار گذاشته می‌شوند تا نودهای کند باعث انسداد صف نهایی Xray نشوند.

### مرحله ۴: تأیید نهایی توسط هسته Xray بدون رابط گرافیکی (اتاق VIP)
* **هدف:** اثبات نهایی مفهوم. تأیید اینکه IP می‌تواند ترافیک وب‌سوکت VLESS را از طریق پیکربندی پروکسی خاص کاربر با موفقیت مسیریابی کند.
* **مکانیسم:**
  ۱. موتور یک کپی عمیق از پیکربندی (Config) کاربر را در حافظه ایجاد می‌کند.
  ۲. به صورت پویا پارامترهای غیرضروری (مانند بلوک‌های `routing` یا `dns`) را حذف می‌کند تا از باگ کرش کردن فایل `geosite.dat` جلوگیری کند.
  ۳. یک نمونه ایزوله و بدون رابط گرافیکی (Headless) از باینری رسمی `Xray-core` را با استفاده از `asyncio.create_subprocess_exec` به یک پورت محلی تصادفی (بین ۲۰۰۰۰ تا ۵۰۰۰۰) متصل می‌کند.
  ۴. یک نشست پروکسی با `aiohttp` یک اتصال زنده را از طریق نمونه محلی تازه ایجاد شده Xray به `cp.cloudflare.com` هدایت می‌کند تا پینگ دقیق و رمزنگاری‌شده (TTFB) را محاسبه کند.
  ۵. **تخریب دقیق:** پس از تأیید، اسکریپت پایتون یک سیگنال `SIGTERM` (و به عنوان پشتیبان `SIGKILL`) می‌فرستد تا فرآیند زیرمجموعه را فوراً از بین ببرد و اطمینان حاصل کند که هیچ نشت حافظه یا فرآیند زامبی باقی نمی‌ماند.

---

## 🧠 بهینه‌سازی‌های الگوریتمی در پس‌زمینه

### ۱. تصادفی‌سازی طبقه‌بندی‌شده ساب‌نت‌ها
تصادفی‌سازی ساده آی‌پی‌ها اغلب باعث می‌شود اسکنر در ساب‌نت‌های عظیم و به شدت مسدود شده (مانند محدوده‌های `104.16.x.x`) "گیر" کند. این الگوریتم فایل `ipv4.txt` را نگاشت کرده و شبکه‌ها را بر اساس اکتت اول (First Octet) آن‌ها دسته‌بندی می‌کند. سپس به صورت تصادفی از *بین* این گروه‌ها انتخاب می‌کند و یک توزیع واقعاً جهانی از دیتاسنترهای تست‌شده را تضمین می‌کند.

### ۲. حلقه بازخورد "ساب‌نت‌های داغ" (Hot-Subnet)
هنگامی که هندشیک TLS در مرحله ۲ موفقیت‌آمیز باشد، موتور تشخیص می‌دهد که بلوک ساب‌نت `/24` در حال حاضر توسط فایروال مسدود نیست. سپس این بلوک را به یک حافظه پنهان (Cache) چرخشی از "ساب‌نت‌های داغ" اضافه می‌کند. ژنراتور مرحله ۱ با احتمال ۳۰ درصد IP را به جای لیست جهانی، از این حافظه پنهان داغ استخراج می‌کند که این امر به طور مؤثری دیتاسنترهای موفق را برای یافتن نودهای کارآمدتر "استخراج" می‌کند.

### ۳. کاهش فشار ناهمگام (Backpressure Mitigation)
از آنجا که راه‌اندازی و مسیریابی یک تونل وب‌سوکت توسط هسته Xray حدود ۱.۵ ثانیه طول می‌کشد، مرحله ۴ به طور قابل توجهی کندتر از مرحله ۱ است. برای جلوگیری از مصرف گیگابایت‌ها حافظه RAM توسط برنامه از طریق صف‌بندی میلیون‌ها آی‌پی، این موتور از `asyncio.Queue(maxsize=X)` محدود شده استفاده می‌کند. اگر صف‌ها پر شوند، مراحل قبلی از رهاسازی غیرمسدودکننده (`asyncio.TimeoutError`) استفاده کرده و IPهای اضافی را دور می‌اندازند تا خط لوله‌ای کاملاً روان و بدون لگ حفظ شود.

### ۴. تجزیه دوطرفه پیکربندی
این اسکنر دارای یک موتور قدرتمند RegEx است که می‌تواند فایل‌های استاندارد JSON اکستری (Xray) و رشته‌های URI آدرس (مثلاً `vless://`) را متقابلاً به یکدیگر ترجمه کند.
* اگر کاربر یک لینک URI وارد کند، پارسر UUID، SNI، ALPN، Path و Host را استخراج کرده و به صورت ریاضی دیکشنری پیچیده و تودرتوی JSON مورد نیاز برای راه‌اندازی هسته Xray را بازسازی می‌کند.
* برعکس، هنگامی که یک آی‌پی تأیید می‌شود، موتور درخت نحو JSON را پیمایش می‌کند تا IP جدید را مجدداً در یک رشته `vless://` کاملاً فرمت شده و قابل اشتراک‌گذاری کدگذاری کند.

---

## 💻 داشبورد رابط کاربری متنی (TUI)

این رابط کاربری که بر روی فریم‌ورک مدرن `Textual` ساخته شده است، در یک ترد (Thread) کاملاً مستقل از موتور اسکن ناهمگام اجرا می‌شود.

* **به‌روزرسانی زنده جدول:** همزمان با عبور IPها از مرحله ۴، آن‌ها به یک لیست تاپل داخلی اضافه می‌شوند. رابط کاربری جدول را پاک می‌کند، **امتیاز کیفیت (Quality Score)** را مجدداً محاسبه می‌کند (سرعت × ۱۰۰۰ / پینگ_Xray)، کل لیست را به ترتیب نزولی مرتب کرده و بلافاصله جدول داده‌ها را دوباره رسم می‌کند.
* **واکنش‌پذیری کامپوننت‌ها:** با استفاده از دکوراتورهای `@on(Input.Changed)`، قرار دادن یک لینک یا تنظیم محدودیت آی‌پی‌های هدف (Target IPs) بلافاصله و بدون نیاز به راه‌اندازی مجدد برنامه، وضعیت‌های داخلی موتور را به‌روزرسانی می‌کند.

---

## 🛠️ عیب‌یابی و مشارکت

اگر با مشکلی مواجه شدید، لطفاً قبل از باز کردن یک Issue در گیت‌هاب، از ابزارهای تشخیصی داخلی استفاده کنید:

۱. **لاگر حرفه‌ای:** روی دکمه **"Save Log"** در داشبورد TUI کلیک کنید. ماژول `logging` پایتون به طور خاموش در پس‌زمینه اجرا می‌شود و خروجی‌های استاندارد، تایم‌اوت‌های سوکت، خطاهای تجزیه JSON و ردیابی‌های خام Subprocess را ثبت می‌کند.

۲. **بررسی `scanner_error.log`:** این فایل را در پوشه اصلی برنامه بررسی کنید. این فایل به صراحت به شما می‌گوید که آیا `xray.exe` وجود ندارد، یک پورت موفق به اتصال نشده است، یا خطای دیگری رخ داده است.

۳. **باز کردن Issue:** به برگه [GitHub Issues](https://github.com/amirrezas/WaldonCFscanner-python/issues) بروید و فایل `scanner_error.log` خود را پیوست کنید. مشارکت‌های برنامه‌نویسان دیگر، Pull Requestها و گزارش‌های باگ به شدت استقبال می‌شوند!

---

*سلب مسئولیت: این نرم‌افزار به عنوان یک ابزار متن‌باز برای عیب‌یابی شبکه، بهینه‌سازی تأخیر (Latency) و تضمین دسترسی آزاد به اینترنت ارائه شده است. توسعه‌دهندگان هیچ مسئولیتی در قبال سوءاستفاده از این ابزار ندارند. لطفاً مسئولانه و مطابق با قوانین شبکه محلی خود از آن استفاده کنید.*